
  # Wanderwork Landing

  This folder contains the newer Figma landing-page export used as reference for the canonical `Wanderwork/` landing app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
